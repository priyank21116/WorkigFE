import { createSlice } from "@reduxjs/toolkit";

const CinitialState={

}

export const Cmlice = createSlice({
      name:'Cm',
      CinitialState,
      reducers:{

      }
})


export const { } = Cmlice.actions

export default Cmlice.reducer